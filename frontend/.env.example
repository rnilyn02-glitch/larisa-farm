# Backend API
NEXT_PUBLIC_API_URL=https://api.ramirezi1.online

# Admin credentials — set in .env.local, never commit real values
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me
ADMIN_JWT_SECRET=change-me-to-random-32-char-string
